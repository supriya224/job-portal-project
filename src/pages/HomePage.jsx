import React from 'react'
import classNames from "classnames";
import styles from "./page.module.css";

export const HomePage = () => {
  return (
    <div className={classNames(styles.about_page)}>HomePage</div>
  )
}
